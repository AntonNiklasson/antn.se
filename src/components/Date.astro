---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const d = new Date(date);
---

<time class="semibold text-sm text-gray-500" datetime={d.toISOString()}>
	{
		d.toLocaleDateString("sv-SE", {
			year: "numeric",
			month: "numeric",
			day: "numeric",
		})
	}
</time>
