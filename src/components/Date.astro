---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const d = new Date(date);
---

<time class="text-gray-600 text-sm font-medium" datetime={d.toISOString()}>
	{
		d.toLocaleDateString("sv-SE", {
			year: "numeric",
			month: "numeric",
			day: "numeric",
		})
	}
</time>
