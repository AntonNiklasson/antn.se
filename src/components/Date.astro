---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const d = new Date(date);
---

<time class="text-gray-500 text-sm semibold" datetime={d.toISOString()}>
	{
		d.toLocaleDateString("sv-SE", {
			year: "numeric",
			month: "numeric",
			day: "numeric",
		})
	}
</time>
